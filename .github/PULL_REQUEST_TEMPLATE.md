# Task

<!-- Task ID + one-line goal, e.g. M5-T2 — paste simulation path 1. Link plan doc section. -->

## Changes

<!-- What was built/changed, 2–5 bullets. -->

## Acceptance evidence

<!-- One row per acceptance criterion from the plan doc. Evidence = test name, measured value, or `deferred → manual QA`. -->

| Criterion | Evidence |
| --- | --- |
| | |

## Local gates

- [ ] `xcodebuild build` pass
- [ ] `xcodebuild test` pass
- [ ] `/code-review` run, confirmed findings fixed (effort: medium / high)
- [ ] `/verify` run (or N/A: no runtime surface)
- [ ] Golden-set harness pass (prompt changes only)

## Deferred manual QA (owner)

<!-- Concrete steps in Japanese the owner can execute without code knowledge. "None" if fully verified. -->

## Decisions & deviations

<!-- Deviations from the plan spec (plan doc updated in this PR?) and entries added to docs/decisions.md. "None" if none. -->
