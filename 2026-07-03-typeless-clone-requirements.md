# 要件定義書: AI音声入力アプリ(仮称 Koe / TYPELESS クローン)

作成日: 2026-07-03
バージョン: 0.1 (MVP要件)
対象プラットフォーム: macOS(MVP)、iOS / Web(次フェーズ)

## 1. プロダクトビジョン

「話すだけで、丁寧にタイプしたような日本語になる」システムワイド音声入力。生の文字起こしではなく、フィラー除去・言い直し統合・文体統一まで済んだテキストをカーソル位置に直接挿入する。日本語の整形品質で既存の英語圏プロダクト(Typeless / Wispr Flow)を上回ることを差別化軸とする。

## 2. ターゲットユーザー

- 一次: 日本語で大量のテキストを書くナレッジワーカー(メール、Slack、ドキュメント、AIチャットへのプロンプト入力)
- 二次: 開発者(Claude Code / Cursorへの指示出しを音声化)、多言語話者(日英混在発話)

## 3. 機能要件

### 3.1 MVP(P0: これがないと製品にならない)

FR-01 グローバルホットキー起動
任意アプリ上でホットキー(デフォルト: Fn長押し または Cmd+Shift+Space、変更可)を押している間、またはトグルで録音する。押下から録音開始まで200ms以内。

FR-02 ストリーミング文字起こし
録音音声をストリーミングSTTに送信し、部分結果をフローティングHUDに表示する。日本語・英語の自動判別、混在発話に対応。

FR-03 AI整形
文字起こし結果に対して以下を適用したテキストを生成する。
- フィラー除去(えー、あのー、なんか、um等)
- 言い直しの統合(「明日、いや明後日に」→「明後日に」)
- 繰り返し・冗長表現の圧縮
- 句読点・改行・箇条書きの自動整形
- 文体統一(ですます/である、ユーザー設定に従う)
整形は発話終了から1.5秒以内に完了すること(体感目標)。

FR-04 カーソル位置への挿入
フォーカス中のテキストフィールドへ整形済みテキストを挿入する。Accessibility API直接挿入を第一手段、クリップボード+Cmd+Vシミュレーションをフォールバックとする。挿入後、退避したクリップボード内容を復元する。

FR-05 パーソナル辞書
固有名詞・専門用語(例: JasmyLab、SENQ、Avalanche)をユーザーが登録でき、STTのキーワードブーストとLLMプロンプトの両方に注入する。誤変換の学習候補を自動提案する。

FR-06 履歴(ローカル保存)
ディクテーション履歴(生テキスト/整形後テキスト/日時/挿入先アプリ)をローカルDBに保存。検索・再コピー・削除が可能。クラウドには保存しない。

FR-07 オンボーディング
マイク権限・アクセシビリティ権限の取得を誘導し、テストディクテーションまで3分以内に完了できるフロー。

### 3.2 P1(MVP後の早期追加)

FR-08 アプリ別トーンプロファイル
フロントアプリ(Slack / Mail / Notion / ターミナル等)を検出し、トーン(カジュアル/フォーマル/コード向けプレーン)を自動切替。ユーザーがアプリごとに上書き設定可能。

FR-09 選択テキストへの音声コマンド(Ask機能)
テキスト選択状態でホットキーを押すと編集モードになり、「短くして」「英語にして」「敬語にして」等の音声指示で選択範囲を書き換える。読み取り専用テキストには回答をHUDに表示。

FR-10 音声翻訳モード
話した内容を指定言語(日→英、日→中など)に翻訳して挿入。

FR-11 文体パーソナライズ
ユーザーの過去の整形結果と手直しを学習素材として、few-shot例をプロンプトへ動的注入。「パーソナライズ進捗」をUIで可視化。

FR-12 スニペット/音声コマンド
「いつもの挨拶」等の定型文呼び出し、「全部消して」等の編集コマンド。

### 3.3 P2(拡張)

- iOS版(キーボード拡張+メインアプリ)
- Webプレイグラウンド(LP埋め込みデモ、アカウント登録導線)
- Windows版
- チーム共有辞書、SSO、管理コンソール
- 完全ローカルモード(whisper.cpp / Parakeet、オフライン対応)

## 4. 非機能要件

NFR-01 レイテンシ: 発話終了→挿入完了 P50 1.5秒 / P95 3秒以内
NFR-02 プライバシー: 音声・テキストのサーバー保持ゼロ(ゼロリテンション設定のSTT/LLMプロバイダのみ利用)。履歴はデバイス内のみ。学習利用しないことを明文化
NFR-03 安定性: 挿入失敗時は必ずクリップボードに整形結果を残し、HUDに通知(テキスト喪失をゼロにする)
NFR-04 リソース: アイドル時CPU 1%未満、メモリ150MB以下、録音中もファン無音レベル
NFR-05 配布: Developer ID署名+notarization、Sparkleによる自動アップデート
NFR-06 オフライン劣化: ネットワーク断時は録音を継続しローカルにバッファ、復帰後に処理(P1)
NFR-07 課金: 無料枠(月60分または週2,000語)+Pro(月額1,980〜2,980円)。Stripe利用
NFR-08 計測: 起動数、ディクテーション回数/長さ、整形の手直し率、挿入成功率(PostHogまたはGA4、テキスト内容は送信しない)

## 5. スコープ外(MVPでは作らない)

- 会議録音・議事録(MacWhisper領域、別プロダクト)
- 音声エージェントによるOS操作
- Android版
- App Store配布(権限制約のため直接配布とする)

## 6. 成功指標(MVP)

- オンボーディング完了率 70%以上
- D7継続率 40%以上
- 整形結果の無修正送信率 60%以上(手直し率で計測)
- 挿入成功率 99%以上(フォールバック込み)

## 7. リスクと対応

| リスク | 影響 | 対応 |
|---|---|---|
| STT日本語精度が期待未満 | 中核体験の毀損 | ElevenLabs/Deepgram/OpenAIを抽象化レイヤーで差し替え可能に。自前音声でWERベンチを先に取る |
| 整形LLMの過剰書き換え(意図改変) | 信頼喪失 | 整形強度3段階(そのまま/軽く/しっかり)をユーザー選択制に。生テキストも履歴に常時保持 |
| アクセシビリティ権限の心理障壁 | オンボード離脱 | 権限が必要な理由を図解、権限なしでもクリップボードモードで動作 |
| Electron系のアクセシビリティ挿入の不安定さ | 挿入失敗 | ネイティブSwift実装を基本とし、挿入層だけはWeb技術に依存しない |
| クラウドコストの逆ザヤ | 収益性 | ヘビーユーザーの原価約$9/月を前提に価格設定。将来ローカルモードでコスト構造改善 |
