# STATUS — single source of truth for progress

Protocol: see `06-autonomous-workflow.md`. Statuses: `todo` / `in-progress` / `pr(#N)` / `done` / `blocked(<what>)`. Order = execution order; pick the first unblocked `todo`. Update this file in the same PR as the work.

## Now

Environment resolved: **Xcode 26.6 installed, `gh` authenticated, autonomous PR flow proven** (PR #1 merged via CI-green → merge commit). Full test suite runs with `./scripts/test.sh`.

**⚠️ 2026-07-09 — owner action needed: GitHub Actions billing.** PR #30 以降の CI が「payments failed / spending limit」で実行不可（macOS ランナー分数の上限超過とみられる）。github.com → Settings → Billing & plans で解消してください。解消後: 各オープン PR の CI を re-run → green を確認して merge commit でマージ（Claude が次セッションで自動処理可能）。ローカルゲート（build+test+review+verify）は全 PR 通過済み。

**2026-07-09 — hotfix `fix/ws-ping-double-resume`**: `URLSessionWebSocketChannel.ping()` double-resumed its continuation when the pong raced a socket failure (SIGTRAP crash ~1–3 min after launch, reachable since PR-B's launch prewarm; found by M9-T1 runtime verification). Resume latched via `OSAllocatedUnfairLock`; 4-min soak clean. Details: decisions.md session 13.

**2026-07-09 — E2E wiring underway (session 13, /loop autonomous).** The STT+format+paste wiring is split in two: **PR-A (this branch, `feat/e2e-t1-streaming-seams`)** — `AudioCapturing`/`Transcribing` reshaped to streaming (`AsyncStream<Data>` + `onRecordingEnded`), `UtteranceContext.recordingBundleID` captured per press via a new `ContextProviding` collaborator on `SessionCoordinator` (M5-T2 prerequisite closed), new `STTTranscriber` adapter with a finals-tail circuit breaker (`sttStallTimeout` 30s / `STTError.timeout` — a hung socket can no longer wedge the insertion FIFO), `LLMFormatter` reads app context from the utterance instead of a live closure. High-effort review applied: `onRecordingEnded` exactly-once on every exit path (early-finalize/send-failure races could drop a transcribed utterance), `AudioCapturing` stream is throwing so mic faults fail loudly instead of inserting truncated text. 174 tests green. **PR-B (this branch, `feat/e2e-t2-app-wiring`)** — App composition root landed: press-order queue awaiting each `startUtterance()` (batch-B FIFO contract), `HotkeyAudioSource`, Keychain-keyed `SpeechmaticsClient`/`GeminiClient`/`HistoryStore`/`DictionaryStore` construction (async, off the launch critical path — Keychain consent dialog must not freeze the app), missing-key degradation (no Speechmatics → inert hotkey + ⚠︎ `configurationWarning`; no Gemini → raw-insert degrade), `PasteSimulator` consumes `context.recordingBundleID`, project.yml gains KoeStorage/KoeProviders. **Fixed a launch-killing pre-existing bug**: `engine.prepare()` on the empty AVAudioEngine graph in `AudioCaptureEngine.init` raised a swallowed ObjC exception that silently killed hotkey+pipeline wiring on `main` as well (decisions.md session 13). Runtime-verified live: `app_launched → alt_hotkey_registered → pipeline_ready`, stores created, idempotent relaunch. Real dictation E2E = owner QA (mic TCC + AX + Keychain consent, checklist in PR). Then M9-T1 → M4-T3 → M10-T1 → M7-T2 → M10-T2 → M11-T1 → M11-T2 → S3 (see docs/decisions.md session 13).

**2026-07-09 — full-codebase review complete (session 12, owner-directed).** Whole codebase reviewed batch-by-batch A–F; confirmed findings fixed under the standard 1-branch/1-PR gates. Merged: **PR #20** (batch A insertion — AppleScript `terminationHandler`-before-`run` FIFO-stall, cancellation-safe paste choreography, bounded AX reads), **PR #21** (batch B — `startUtterance` FIFO-ordering doc contract), **PR #22** (batch D — orphaned `llmTotalTimeout` now enforced via a new cancellation-correct `Deadline.run` primitive shared by `LLMFormatter` + `SpeechmaticsClient.verify`; `format` re-throws `CancellationError` so a cancelled utterance never inserts). Batches C/E/F: no behavior bugs (reviewed-clean notes + forward-looking M9 callback-split note in `docs/decisions.md`). 164 tests green; `main` CI green. Details: `docs/decisions.md` (session 12 entries).

- **PR #1 (merged)**: SwiftPM package (`KoeKit`/`KoeCore`), Constants, `DictationSession` state machine (M1-T1), CI, scripts.
- **M1-T2** (this branch): `InsertionSerializer` FIFO gate + `SessionCoordinator` + collaborator seams; 18 tests total green.

Bundle ID fixed: **`dev.newt.Koe`**. The app is a runnable menu-bar skeleton (PR #3).

**Running the app** (owner QA): `xcodegen generate` (once, or after `project.yml` changes) → open `Koe.xcodeproj` in Xcode → Run. Or `open` the built `Koe.app`. It shows a mic icon in the menu bar with History/Settings/Pause/Quit; no dictation yet. `Koe.xcodeproj` is git-ignored (regenerate from `project.yml`). Requires `brew install xcodegen`.

Targets: **KoeCore** (pure) + **KoeStorage** (GRDB) + **KoeProviders** (API adapters). **Gemini + Speechmatics keys are in the Keychain and validated.** The formatting pipeline works end-to-end against real Gemini (filler removal / self-correction / punctuation verified live). **M4-T1/T2 done (branch `feat/m4-speechmatics-stt`):** streaming `STTClient` + `SpeechmaticsClient` (WebSocket RT, `EndOfStream` push-to-talk termination, `additional_vocab` injection, invariant-8 clean); 14 new tests, 86 total green. **M2-T1 done (branch `feat/m2-t1-fn-hotkey`):** Fn push-to-talk via CGEventTap — pure `HotkeyEngine`/`FnKey` in KoeCore (+20 tests, 106 total) + `App/FnHotkeyTap.swift` adapter, wired to the menu-bar icon (recording↔normal) for QA; AX-guarded (⚠︎ when untrusted). **M2-T2 done (branch `feat/m2-t2-liveness-alt-hotkey`):** tap liveness (`TapLiveness` pure decision + 60s `Task` in `FnHotkeyTap`; re-enable + Accessibility-revocation → ⚠︎) and alt hotkey (`AltHotkeyMonitor`, ⌥Space via KeyboardShortcuts 3.0.1, no TCC); +3 tests (109 total). **M3-T1 done (branch `feat/m3-t1-audio-engine`):** pure `AudioFormatSpec` + `SessionAudioBuffer` (20-min/≈38MB cap) in KoeCore (+12 tests, 121 total) + `App/AudioCaptureEngine.swift` (AVAudioEngine/AVAudioConverter → 16kHz mono PCM16, `AsyncStream<Data>` ~40ms chunks, App-Nap-guarded, press→first-buffer latency log), wired to both hotkeys. **M3-T2 done (branch `feat/m3-t2-device-switch`):** pure `InputDeviceSelector`/`AudioInputDevice` (prefer built-in / route-change switch decision) + `App/AudioDeviceObserver.swift` (CoreAudio) + engine device-pinning & same-session converter/tap rebind on AirPods drop; +11 tests (132 total); "prefer built-in mic" default ON. **M5-T2 done (merged PR #18):** path-1 paste simulation as `TextInserting`. **M5-T3 done (branch `feat/m5-t3-fallback-chain`):** full fallback chain (path 1 Cmd+V → path 2 AppleScript → path 3 clipboard landing) + per-app overrides; pure `InsertionChain`/`InsertionPathPlanner`/override table fault-injection tested in KoeCore (+12 tests, 158 total). **M5 milestone complete → owner runs `/code-review ultra` + M5 QA checklist.** Next: STT+format+paste end-to-end wiring (needs owner QA; must first thread per-utterance recording bundle id through `UtteranceContext` per the M5-T2 review note, and must `await` each `SessionCoordinator.startUtterance()` in press order — unawaited per-press `Task`s do not guarantee FIFO ticket order, see the batch-B review note in decisions.md); M4-T3 unblocked on the M3 audio buffer (still waits on M9 HUD retry button). Live tests: `KOE_LIVE_TESTS=1 ./scripts/test.sh`; STT live also needs `KOE_STT_SAMPLE_WAV=<16kHz mono PCM16 wav>` (owner-supplied).

## Phase 0 spikes (`01-phase0-spikes.md`)

| Task | Status | Notes |
| --- | --- | --- |
| S1-T1 insertion harness | todo | Needs Accessibility TCC grant at first run (owner clicks dialog) |
| S1-T2 insertion matrix run | todo | Depends S1-T1; partially manual (owner assists per checklist) |
| S2-T1 STT harness + utterance set | blocked(owner: STT API keys, voice recordings) | |
| S2-T2 STT A/B evaluation | todo | Depends S2-T1 |
| S3-T1 golden set v1 | todo | No keys needed to author cases — can start anytime |
| S3-T2 regression harness + LLM A/B | blocked(owner: Gemini/AWS keys) | Harness code can be written before keys |
| S4-T1 E2E latency prototype | todo | Depends S1/S2/S3 outcomes |

## Phase 1 — M0–M3 (`02-phase1-m1-core.md`)

| Task | Status | Notes |
| --- | --- | --- |
| M0-T1 Xcode project + layout + CI | done | SwiftPM pkg + CI (PR #1); Xcode app target via XcodeGen, bundle ID `dev.newt.Koe` (PR #3) |
| M0-T2 status item, Log wrapper, Keychain | done(branch) | `feat/m0-t2-app-shell`: NSStatusItem menu-bar app, Log (StaticString, invariant 4), SecretStore/Keychain; runnable |
| M1-T1 DictationSession actor | done | merged PR #1, 11 tests |
| M1-T2 coordinator + FIFO insertion lock | done | merged PR #2, +7 tests |
| M2-T1 CGEventTap Fn hotkey | done(branch) | `feat/m2-t1-fn-hotkey`, +20 tests; pure `HotkeyEngine` (hold/toggle) + `FnKey` (keycode 63/`maskSecondaryFn`) in KoeCore, `App/FnHotkeyTap.swift` CGEventTap adapter (AX-guarded, consumes Fn, main-run-loop `assumeIsolated`). Immediate `tapDisabled*` re-enable; wired to menu-bar icon for QA. Needs owner QA (AX grant + globe→"Do Nothing"). App builds |
| M2-T2 tap liveness + alt hotkey | done(branch) | `feat/m2-t2-liveness-alt-hotkey`, +3 tests; pure `TapLiveness` (trusted×enabled → healthy/reenable/revoked) in KoeCore; `FnHotkeyTap` 60s liveness `Task` (re-enable + revocation → ⚠︎ via `onRevoked`); `AltHotkeyMonitor` (⌥Space push-to-talk via KeyboardShortcuts 3.0.1, no TCC). App builds. Needs owner QA |
| M3-T1 audio engine + buffer | done(branch) | `feat/m3-t1-audio-engine`, +12 tests (121 total); pure `AudioFormatSpec` (16kHz/mono/PCM16 wire format + chunk-frame math) + `SessionAudioBuffer` (in-memory session audio, 20-min/≈38MB cap → truncate + `.capReached`) in KoeCore; `App/AudioCaptureEngine.swift` (AVAudioEngine prepared-but-stopped, AVAudioConverter → 16kHz Int16, `AsyncStream<Data>` ~40ms chunks, `beginActivity(.userInitiated)`, press→first-buffer latency log). Wired to both hotkeys (start/drain/stop; cap → icon reset). App builds; needs owner QA (mic TCC grant + latency ≤200ms) |
| M3-T2 device-switch survival | done(branch) | `feat/m3-t2-device-switch`, +11 tests (132 total); pure `InputDeviceSelector` + `AudioInputDevice` in KoeCore (`select` = prefer built-in to dodge Bluetooth HFP; `resolveSwitch` = keep/switch on route change). `App/AudioDeviceObserver.swift` (CoreAudio device enumeration + default-input/device-list listeners, UID↔AudioDeviceID resolve). `AudioCaptureEngine` pins the selected device (`kAudioOutputUnitProperty_CurrentDevice`), and on route change/`.AVAudioEngineConfigurationChange` rebinds the converter+tap onto the same `TapState` (buffer+stream survive → transcript spans both devices), notifies `onDeviceSwitched` (M9 HUD hook). Pref `preferBuiltInMic` (UserDefaults, default ON). App builds; needs owner QA (disconnect AirPods mid-utterance) |

## Phase 1 — M4–M6 (`03-phase1-m2-pipeline.md`)

| Task | Status | Notes |
| --- | --- | --- |
| M4-T1 STTClient protocol | done(branch) | `feat/m4-speechmatics-stt`; `STTClient`/`STTEvent`/`STTError` in KoeCore (streaming, prewarm/begin/send/end) |
| M4-T2 primary STT adapter | done(branch) | 〃 +14 tests; `SpeechmaticsClient` (actor, URLSessionWebSocketTask via `WebSocketChannel` seam), `additional_vocab` (full-width kana, ≤1000), invariant-8 no smart-formatting. **`EndOfStream` not the design's fictional `ForceEndOfUtterance`** (decisions.md). Live test opt-in (owner WAV). S2 A/B still separate |
| M4-T3 batch resend + retry UI | done(branch) | `feat/m4-t3-batch-resend`, +15 tests (203 total); `ResilientTranscriber` (KoeCore) が §10.2 ladder を実装 — streaming 失敗（`sttFinalTimeout` 2s の finals tail 超過含む）→ 完全キャプチャ待ち → `SpeechmaticsBatchClient`（`/v2/jobs` multipart, poll, `format=txt`）へ 1 回再送 → double-fault で WAV を `TempAudioStore` に保存し `UnrecoveredUtterance` → coordinator が「未転写セッション」history 行 + `RecoveryHandle` を HUD へ → HUD retry ボタン（`hudRetryDwell` 10s、この状態のみ mouse 透過解除）→ `SessionCoordinator.startRetry` が新 ticket で batch 再試行、成功時に audit 行と WAV を削除。ソケット死後の音声も再送対象（mic は継続、fault-injection テスト済み）。Needs owner QA（実ネットワーク断での再送/リトライ） |
| M5-T1 preflight / ContextProvider | done(branch) | `feat/m5-t1-preflight`, +7 tests; pure decision (secure→block no-clipboard, app-changed→hold). AX/IsSecureEventInput reads are the M5-T2 runtime part |
| M5-T2 paste simulation path 1 | done(branch) | `feat/m5-t2-paste-simulation`, +13 tests (159 total); pure `PasteboardMarkers` (transient/auto-generated/source + private session-UUID type, no ConcealedType) + `PasteTextPreparer` (terminal-family detect + trailing-newline strip + interior-newline warn, `\r\n`-grapheme safe) + `PasteVerification` (best-effort AX verdict) in KoeCore. `App/PasteSimulator.swift` (`TextInserting`: preflight → snapshot all NSPasteboardItems → write marked payload → 100ms pre-delay → synth Cmd+V via `CGEventSource(.privateState)`→`.cghidEventTap` → 300ms settle → best-effort AX verify → restore only if session marker survives) + `App/InsertionContextProvider.swift` (`IsSecureEventInputEnabled`, focused-element `kAXSecureTextFieldSubrole`, frontmost bundle id; AX read-only) + `App/PasteKeyResolver.swift` (`TISCopyCurrentKeyboardLayoutInputSource`+`UCKeyTranslate` → "v" keycode, fallback 9). Secure ctx → block (no clipboard); app-change/verify-fail → leave marked text on clipboard (`.clipboardFallback`). DEBUG QA menu item drives a delayed test paste (pipeline not yet wired). App builds; needs owner QA (paste into Slack/TextEdit/Terminal/password field; ⌘Z removes exactly the text; clipboard-manager non-recording) |
| M5-T3 paths 2–3 + per-app overrides | done(branch) | `feat/m5-t3-fallback-chain`, +12 tests (158 total); pure `InsertionPath`/`AppInsertionOverride`/`InsertionOverrideTable`/`InsertionPathPlanner` + `InsertionChain.run` (ordered walk, stop-at-first-`.landed`, floor `.clipboardFallback`; `isolated` param inherits caller isolation) in KoeCore — fault-injection unit-tested (block path1→path2→path3). `PasteSimulator` restructured into the chain: path 1 (Cmd+V) / path 2 (`osascript` System Events `keystroke "v" using command down`, bounded by `insertionStageTimeout` via a watchdog that terminates the process) / path 3 (leave marked text for ⌘V — designed landing). Per-app override (preferred path + extra pre-delay) resolved against frontmost bundle id (table empty until M10 settings). Verify-fail/write-fail/post-fail → `.advance` (no restore, payload kept for next path). 3 DEBUG QA menu items force each path. App builds; **milestone end** → owner: `/code-review ultra` + QA (path 1/2/3 landings; Automation TCC prompt for path 2; per-app override validated in M10) |
| M6-T1 LLMClient protocol + adapters | done(branch) | `feat/m6-t1-gemini`, +8 tests; new KoeProviders target; GeminiClient (thinking off, ~0.8s by curl) + LLMFormatter (Formatting seam). Live-verified formatting. Streaming + long-form chunking deferred; S3 golden-set quality A/B still to run |
| M6-T2 prompt assembly (versioned) | done(branch) | `feat/m6-t2-prompt-assembly`, +9 tests; template v1.0.0 (ja), content-hash versioning, injection boundary |
| M6-T3 chunking/validation/degradation | done(branch) | `feat/m6-t3-chunk-validate`, +13 tests; chunker + OutputValidator (empty/summarization/leakage → degrade). Async retry/timeout ladder lands with M6-T1 |

## Phase 1 — M7–M11 (`04-phase1-m3-experience.md`)

| Task | Status | Notes |
| --- | --- | --- |
| M7-T1 history schema + write-ahead | done(branch) | `feat/m7-t1-history-store`, +8 tests; GRDB in new KoeStorage target; write-ahead + FTS5 trigram + LIKE fallback for short JP queries + retention |
| M7-T2 history UI | done(branch) | `feat/m7-t2-history-ui`, +1 test (205 total); `App/HistoryView`（逆時系列 200 件/検索は `HistoryStore` の FTS+LIKE ハイブリッド、行 = formatted（タップで raw 展開）+ timestamp + app + 結果バッジ、copy/👎/delete、全削除は確認ダイアログ）。👎 は新規 `HistoryStore.updateFeedback`（rework-rate proxy、トグル、round-trip テスト済み）。未転写行（M4-T3）は専用表示。StatusItemController の placeholder を実メニュー化（⌘Y）、`AppDelegate.openHistory` が NSWindow+NSHostingController を生成、store は SettingsHub 経由（初期化前は説明付き空状態）。retention 実行は M10-T1 で起動時に配線済み。削除の FTS 整合は store 側テストで担保済み。Needs owner QA |
| M8-T1 dictionary store + dual feed | done(branch) | `feat/m8-t1-dictionary-store`, +9 tests; GRDB CRUD + dual serializers (LLM prompt entries / STT vocab). Editor UI is M10/settings |
| M9-T1 HUD panel | done(branch) | `feat/m9-t1-hud-panel`, +10 tests (184 total); pure `HUDModel`/`HUDReducer` (phase + transient notice 2 層、dwell ポリシー込み) in KoeCore/HUD + `DictationUIObserving` seam on `SessionCoordinator` (utteranceBegan/Landed/Failed + per-utterance `DictationSession.states` stream) + `STTTranscriber.onPartial` side channel (HUD live line 専用、永続化・ログなし)。`App/HUDPanelController` (non-activating borderless NSPanel、`.statusBar` level、all-Spaces+fullScreenAuxiliary、`ignoresMouseEvents`、bottom-center) + `App/HUDView` (SwiftUI)。`AudioCaptureEngine` の `onCapReached` を `onCaptureFault` と分割（batch-C M9 ノート解消 — route-change 失敗が「20分上限」表示になる誤りを防止）。retry ボタンは M4-T3。Needs owner QA（フォーカス非奪取・IME 非干渉・フルスクリーン上の表示） |
| M10-T1 settings | done(branch) | `feat/m10-t1-settings`; SwiftUI 設定画面 4 タブ（一般/辞書/アプリ別/APIキー）。`AppSettings`（typed UserDefaults、`AppDefaultsKey` 拡張: writingStyle/degradedToClipboard/historyRetentionDays/telemetryOptOut/fnHotkeyEnabled/insertionOverrides）。全設定ライブ反映: 文体+prompt 辞書は `LLMFormatter` の provider closure 化（PR-B の起動スナップショットギャップ解消）、per-app override は `PasteSimulator.updateOverrides`、LLM 失敗時 clipboard-only は degraded 出力の挿入経路で消費（secure preflight 優先）、Fn トグルは tap start/stop、retention は起動時+変更時に `deleteOlderThan`、⌥Space は `KeyboardShortcuts.Recorder`、launch-at-login は `SMAppService`。API キーは `SecretStore`/Keychain のみ（invariant 5、CI grep green）。辞書エディタ（M8-T1 store の CRUD UI）。telemetryOptOut は M10-T2 消費者向けに格納のみ。Needs owner QA（UI 操作の round-trip 確認） |
| M10-T2 metrics pipeline | done(branch) | `feat/m10-t2-metrics`, +6 tests (211 total); `DictationSample`（数値のみ、invariant 4 — スキーマ監査テストで本文列ゼロを担保）+ `MetricsRecording` seam を coordinator の outcome 後に fire-and-forget 配線。`MetricsStore`（GRDB metrics.sqlite: durations/outcome/degraded/app/promptVersion/provider）。`AppMetricsRecorder` が telemetryOptOut をサンプル毎に消費し `PromptTemplate.current.version` を刻印。設定「統計」タブ: 直近100件のセグメント別 P50/P95（nearest-rank `Percentiles`）+ E2E 目標比（1500/3000ms 超過は赤）+ 👎率（`HistoryStore.feedbackStats`）+ 30秒内再発話率（`redictationRate`）。⌘Z proxy は tap のマスク拡張が必要なため見送り（decisions 記録）。Needs owner QA |
| M11-T1 onboarding flow | done(branch) | `feat/m11-t1-onboarding`; `App/OnboardingView` 6 ステップ（①APPI consent: プロバイダ名+国+no-training+ローカル保存明記 ②mic: requestAccess+deep link+2s poll 自動前進 ③AX: AXIsProcessTrustedWithOptions+2s poll → 付与直後に tap arm 試行、既知の失敗 quirk は「再起動して続行」④hotkey: Fn 既定+🌐「何もしない」案内+キーボード設定 deep link+⌥Space Recorder ⑤テスト入力: アプリ内 TextField に実パイプラインで挿入、非空で成功表示 ⑥login-item opt-in+メニューツアー）。全ステップ skippable、`onboardingCompleted` defaults、初回起動で自動表示、メニュー「セットアップをやり直す」で再実行。`FnHotkeyTap.start()` を冪等化（tap 非 nil なら no-op — batch-B single-start 性を構造的に保持、M11 re-arm 対応）。Needs owner QA（tccutil reset 込みのフルフロー ≤3min） |
| M11-T2 permission revocation handling | todo | |
| Phase 1 exit gate | todo | Owner: 2-week dogfood + `/code-review ultra` + checklist in `04` |

## Owner-blocked items

Owner-facing step-by-step guide (Japanese): **`docs/owner-guide.md`**.

- [x] Install Xcode (26.6) + `xcode-select`
- [x] `gh auth login`
- [x] Bundle ID decided: `dev.newt.Koe`
- [ ] **[critical path] API keys: Gemini (paid tier) + Speechmatics** → stored in Keychain (`security add-generic-password -s dev.newt.Koe -a geminiAPIKey/speechmaticsAPIKey`). Unblocks M4/M6-T1 and the first vertical slice.
- [ ] Phase 0 A/B keys (later): Deepgram / Soniox / AWS Bedrock (Tokyo)
- [ ] S2 utterance recordings (~100 clips; script list provided by Claude on request)
